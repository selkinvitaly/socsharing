"use strict";

// list of errors
let errors = new Map();

errors.set(0, "you haven't specified the list of services")
      .set(1, "you haven't specified any support service");

export default errors;
